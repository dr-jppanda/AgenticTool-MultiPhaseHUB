Process every PDF file inside [Current Floder]. For each PDF, extract every figure, vector/chart graphic, and table it contains. For each extracted element:

Crop tightly around the visual element, but include its caption (e.g., "Figure 3: ...", "Table 2: ...") in the crop.
Save each cropped element as its own individual PDF file.
Name each file after its caption/label (e.g., Figure_3.pdf, Table_2.pdf); if no caption exists, use a sequential name like figure_01.pdf.
Skip pure body text — only extract visual/tabular content and its caption.

Organize the output as follows:

Create one subfolder per source PDF, named exactly after that PDF (without the .pdf extension).
Save all extracted elements from a given PDF into its corresponding subfolder.
All these subfolders should sit inside a single parent output folder (e.g., extracted_output/).